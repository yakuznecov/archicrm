// help functions for table staff

const Surname = (cell) => {
	return cell.value ? cell.value : '';
};

const Name = (cell) => {
	return cell.value ? cell.value : '';
};

const SecondName = (cell) => {
	return cell.value ? cell.value : '';
};

const Position = (cell) => {
	return cell.value ? cell.value : '';
};

const Email = (cell) => {
	return cell.value ? cell.value : '';
};

const Department = (cell) => {
	return cell.value ? cell.value : '';
};

const Hours = (cell) => {
	return cell.value ? cell.value : '';
};

const Bonus = (cell) => {
	return cell.value ? cell.value : '';
};

const Description = (cell) => {
	return cell.value ? cell.value : '';
};


export {
	Surname,
	Name,
	SecondName,
	Position,
	Email,
	Department,
	Hours,
	Bonus,
	Description
};